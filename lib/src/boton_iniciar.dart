import 'package:flutter/material.dart';

class BotonIniciar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: ElevatedButton(onPressed: (){}, child: Text("iniciar calculo")),
    );
  }
}